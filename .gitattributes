# Normalize all text files to use LF line endings
* text=auto

# Force LF line endings for shell scripts
*.sh text eol=lf

# Force LF line endings for Python files
*.py text eol=lf

# Force CRLF line endings for batch files (if needed)
*.bat text eol=crlf
